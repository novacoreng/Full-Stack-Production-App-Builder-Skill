# Process Flows

For every flow document:

User action → UI → validation → API → auth/authz → backend logic → database → external service → response → UI update → success/error/recovery.

## Flow template
- Flow ID:
- Persona:
- Preconditions:
- Trigger:
- Steps:
- Success state:
- Failure states:
- Recovery/retry:
- Offline/interruption behavior:
- Frontend coverage:
- Backend coverage:
- DB coverage:
- Auth coverage:
- Test coverage:
