# Security
Audit authentication, authorization, RLS, secrets, API exposure, CORS/CSRF, XSS/injection, validation, uploads, rate limiting, sessions/tokens, dependencies, logs, and third-party permissions. Never commit credentials, private keys, service accounts, or environment secrets.
