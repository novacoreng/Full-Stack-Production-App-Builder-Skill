# Deployment
Separate development, test/staging, and production. Verify environment variables and secrets, migrations, builds, domains, SSL, monitoring, backups, and rollback. Never claim deployment success without evidence. Run post-deploy health and error checks.
