# Database
Model entities, relationships, keys, constraints, indexes, enums, audit fields, security/RLS, retention, deletion, export, archive/restore, and migrations. Use ordered migrations and test schema changes.
