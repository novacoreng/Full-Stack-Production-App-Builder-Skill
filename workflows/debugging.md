# Debugging
Never stop at the first error. Inspect configuration, dependencies, types, imports, state, routing, API contracts, backend, database, auth, styling, builds, platform configuration, tests, and runtime behavior. Record BUG ID, file/line, expected, actual, root cause, fix, and verification. Repeat until clean.
